import React from 'react';
import { View, Text } from 'react-native';

export default function MarketScreen() {
    return (
    <View style={{ flex: 1, padding: 20 }}>
        <Text>Página del Market</Text>
    </View>
    );
}